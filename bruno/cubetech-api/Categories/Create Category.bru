meta {
  name: Create Category
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/categories
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "New Category",
    "description": "A test category created via Bruno"
  }
}

vars:pre-request {
  base_url: 
}
